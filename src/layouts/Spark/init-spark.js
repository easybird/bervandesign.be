module.exports = require('react-spark-scroll-rekapi')({
  invalidateAutomatically: true,
  // actionProps: {
  //   log: {
  //     down(o) {
  //       console.log(`spark: scrolling down, hit: ${o.val}`);
  //     },
  //     up(o) {
  //       console.log(`spark: scrolling up. hit: ${o.val}`);
  //     }
  //   }
  // }
// we could add additional options in here...

// setup: {...}
// formulas: {...}
// actionProps: {...}
});
